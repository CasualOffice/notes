//! # sync-core (DORMANT stub)
//!
//! The dormant sync seam of **Data Model §11.2** and **HLD §4** (Architecture §13.1
//! decision #7): tables are a materialized projection of the op-log; a future
//! Loro/blind-relay sync ships op ranges with no re-model. **Compiled but not wired
//! into any write path in v1.**

#![forbid(unsafe_code)]
