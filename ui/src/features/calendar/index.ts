// feature: calendar
// Unified read-mostly agenda: tasks/reminders/meetings projected into events
// (Feature Specs §3.4, §6; calendar crate projection fns).
export { CalendarView } from "./CalendarView";
